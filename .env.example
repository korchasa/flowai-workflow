# Optional: Anthropic API key for Claude CLI (alternative to OAuth via `claude login`)
# ANTHROPIC_API_KEY=sk-ant-...

# Required: GitHub token (PAT with `repo` scope) for gh CLI
GITHUB_TOKEN=ghp_...
